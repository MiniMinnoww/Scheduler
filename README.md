# TO RUN
apologies we havent really had time to make a readme

you need to `npm install` in the src folder then `npm run dev` to set up frontend

then you need to run the flask app from app.py inside the api folder

hopefully that works lol :)